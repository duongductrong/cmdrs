{
  "type": "module",
  "name": "@pmdrs/core",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scope": "pnpm --filter=@pmdrs/scope",
    "scope:dev": "pnpm --filter=@pmdrs/scope dev",
    "scope:build": "pnpm --filter=@pmdrs/scope build",
    "icons": "pnpm --filter=@pmdrs/icons",
    "icons:dev": "pnpm --filter=@pmdrs/icons dev",
    "icons:build": "pnpm --filter=@pmdrs/icons build",
    "ui": "pnpm --filter=@pmdrs/ui",
    "ui:dev": "pnpm --filter=@pmdrs/ui dev",
    "build:ui": "pnpm --filter=@pmdrs/ui build",
    "prompt": "pnpm --filter=@pmdrs/prompt",
    "prompt:dev": "pnpm --filter=@pmdrs/prompt dev",
    "build:prompt": "pnpm --filter=@pmdrs/prompt build",
    "playground": "pnpm --filter=@pmdrs/playground",
    "playground:dev": "pnpm --filter=@pmdrs/playground dev",
    "dev": "npm-run-all --parallel ui:dev playground:dev icons:dev prompt:dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
